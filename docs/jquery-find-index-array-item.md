# jQuery 查找数组项的索引

> 原文：<https://www.sitepoint.com/jquery-find-index-array-item/>

简单的 jQuery 代码片段，用于在给定项目值的情况下查找数组项目的索引。这有点像。NET 数组。FindIndex 方法，但使用 in 数组函数。

```
//syntax
jQuery.inArray( value, array )

//example
jQuery.inArray("Sam", array)
```

## 分享这篇文章